version: '3.8'

services:
  web:
    build: .
    container_name: pablo-web
    ports:
      - "5000:5000"
    environment:
      - DB_PATH=/app/data/pablo.db
    volumes:
      - ./data:/app/data
    restart: always
